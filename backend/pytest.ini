[pytest]
asyncio_mode = auto
testpaths = tests test_api.py test_e2e.py
python_files = test_*.py
markers =
    slow: marks tests as slow (AI generation etc.)
    auth: marks tests that need authentication
    e2e: marks end-to-end tests
